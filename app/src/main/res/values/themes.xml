<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DiplomFinal" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Основные цвета -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/secondary</item>
        <item name="colorSecondary">@color/accent</item>
        
        <!-- Фоны -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        
        <!-- Статус бар и навигация -->
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Цвета текста -->
        <item name="android:textColorPrimary">@color/on_background</item>
        <item name="android:textColorSecondary">@color/on_surface</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorOnSecondary">@color/on_primary</item>
        <item name="colorOnBackground">@color/on_background</item>
        <item name="colorOnSurface">@color/on_surface</item>
        
        <!-- Стили компонентов -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="toolbarStyle">@style/Widget.App.Toolbar</item>
        <item name="cardViewStyle">@style/Widget.App.CardView</item>
    </style>

    <style name="Theme.Diplom_Final.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.DiplomFinal.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/primary</item>
        <item name="android:textColorSecondary">@color/on_surface</item>
        <item name="android:background">@color/background</item>
    </style>

    <style name="Theme.DiplomFinal.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:background">@color/surface</item>
        <item name="android:textColor">@color/on_surface</item>
    </style>

    <!-- Стиль кнопок -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/on_primary</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Стиль тулбара -->
    <style name="Widget.App.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">@color/background</item>
        <item name="titleTextColor">@color/primary</item>
        <item name="subtitleTextColor">@color/on_surface</item>
    </style>

    <!-- Стиль карточек -->
    <style name="Widget.App.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/surface</item>
        <item name="cardElevation">4dp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Стиль экшнбара -->
    <style name="Widget.App.ActionBar" parent="Widget.MaterialComponents.ActionBar.Surface">
        <item name="background">@color/background</item>
        <item name="titleTextStyle">@style/TextAppearance.App.TitleLarge</item>
    </style>

    <!-- Стиль текста заголовка -->
    <style name="TextAppearance.App.TitleLarge" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/primary</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:letterSpacing">0.05</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!-- Стиль для заголовков упражнений -->
    <style name="TextAppearance.App.ExerciseTitle" parent="TextAppearance.MaterialComponents.Headline5">
        <item name="android:textSize">28sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/primary</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:letterSpacing">0.05</item>
        <item name="android:layout_marginBottom">24dp</item>
    </style>

    <!-- Стиль для подзаголовков в деталях упражнений -->
    <style name="TextAppearance.App.ExerciseSubtitle" parent="TextAppearance.MaterialComponents.Subtitle1">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/primary</item>
        <item name="android:layout_marginBottom">12dp</item>
    </style>

    <!-- Стиль для текстовых блоков в деталях упражнений -->
    <style name="TextAppearance.App.ExerciseText" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/on_background</item>
        <item name="android:background">@color/surface</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_marginBottom">24dp</item>
    </style>
</resources>